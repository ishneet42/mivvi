fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2
# openai >= 1.54 no longer passes `proxies=` to its internal httpx client.
# Older versions + newer httpx blow up with:
#   Client.__init__() got an unexpected keyword argument 'proxies'
# Pin both to a known-good combination.
openai>=1.54,<2
httpx>=0.27,<0.28
python-dotenv==1.0.1
Pillow==10.4.0
